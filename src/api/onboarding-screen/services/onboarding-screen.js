'use strict';

/**
 * onboarding-screen service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::onboarding-screen.onboarding-screen');
